public class Driver {
    public static void driver(String[] args) {

        // Canned Beans product that doesn't expire
        Product cannedBeans = new Product();

        cannedBeans.setSkuNum(100629175);
        cannedBeans.setProductName("Canned Beans");
        cannedBeans.setProductCost(5.99);
        cannedBeans.setInStock(16);
        cannedBeans.setToOrder(3);
        cannedBeans.setSpecialInstructions("BE CAREFUL!!!!!!!!!!!!!!");

        // Canned corn that doesn't expire

        Product cannedCorn = new Product();

    }
}


